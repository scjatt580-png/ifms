<?php error_reporting(E_ALL); ini_set("display_errors", 1); require_once __DIR__ . "/config/auth.php"; echo "Auth loaded. Logged in: " . (isLoggedIn() ? "yes " . getUserRole() : "no") . "\n"; if (!isLoggedIn()) { echo "NOT LOGGED IN - would redirect"; exit; } requireRole("admin"); echo "PASSED ROLE CHECK\n"; $db = getDB(); $totalEmployees = $db->query("SELECT COUNT(*) FROM employees WHERE is_active = 1")->fetchColumn(); echo "Employees: $totalEmployees\n"; echo "DONE."; ?>
